message(STATUS "*** Create Zinet {argument_pretty_name} executable ***")

add_executable({argument_name}
	main.cpp
	
	{argument_headers}
	
	{argument_inl}
	
	{argument_source}
)

target_compile_features({argument_name} PUBLIC {argument_cpp_standard})

set_target_properties({argument_name} PROPERTIES LINKER_LANGUAGE CXX)
	
target_compile_definitions({argument_name} PUBLIC GTEST_LINKED_AS_SHARED_LIBRARY)

set_target_properties({argument_name}
    PROPERTIES
	LINKER_LANGUAGE CXX
	INTERFACE_INCLUDE_DIRECTORIES ${CMAKE_CURRENT_SOURCE_DIR}/include/
	INCLUDE_DIRECTORIES ${CMAKE_CURRENT_SOURCE_DIR}/include/
    ARCHIVE_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/archive/"
    LIBRARY_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/lib"
    RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/runtime"
)

target_include_directories({argument_name} PUBLIC {argument_include_dirs})

target_compile_definitions({argument_name} PRIVATE {argument_macros})

target_link_libraries({argument_name} PUBLIC {argument_link_libraries_string})
